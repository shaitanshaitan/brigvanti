# Set this in the Netlify dashboard (Site settings > Environment variables),
# NOT in the committed code.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: override the model (defaults to claude-sonnet-5).
# For lower-cost scoring you could use claude-haiku-4-5-20251001.
CLAUDE_MODEL=claude-sonnet-5
